const React = require('react');
const ReactDOM = require('react-dom');

const PostForm = require('./components/form');

console.log("Kill me");

ReactDOM.render(PostForm, document.getElementById('app'))